source 'https://rubygems.org'

gem 'rails', '3.2.7'

gem 'thin'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

# database-related
gem 'sqlite3'
gem 'mysql2'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
  gem 'bootstrap-sass', '~> 2.0.4.0'
  # for icons:
  # gem 'font-awesome-sass-rails'
end

gem 'jquery-rails'
# gem 'jquery-ui-rails'

# for encrypting user passwords
gem 'bcrypt-ruby', :require => 'bcrypt'

# for views
gem 'erector', '~> 0.9.0' , :git => 'git://github.com/egonSchiele/erector.git'

# dont log assets stuff, too much noise
gem 'quiet_assets', :group => :development

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
gem 'debugger'

# for monitoring the site
# gem 'newrelic_rpm'

# Database-independent dumps, so you can convert sqlite dbs to mysql dbs
#gem 'yaml_db'
gem 'factory_girl_rails'
